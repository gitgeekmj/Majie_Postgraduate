<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.studyjams.s1.sj19.majie"
    android:versionCode="2"
    android:versionName="1.1">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launch1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisciplineActivity" />
        <activity android:name=".DefineTarget" />
        <activity android:name=".SchoolBasicInfo" />

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".EncouageMusic" />
        <activity android:name=".TimePlan" />
        <activity android:name=".ReviewPlan" />
        <activity android:name=".LifeTips" />
        <activity
            android:name=".BookCommend"
            android:label="@string/title_activity_book_commend"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BookComend"
            android:label="@string/title_activity_book_comend"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>